# Dépendances d'exécution — toutes gratuites, aucune API distante.
PySide6>=6.7,<7          # interface graphique (LGPL)
jsonschema>=4.23         # validation stricte du JSON produit par Claude
python-dateutil>=2.9     # arithmétique de dates / récurrences
icalendar>=6.0           # export .ics (Phase 6)
