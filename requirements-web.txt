# Dépendances de l'API web SEULEMENT (Vercel) — surtout pas PySide6.
# Le runtime Python de Vercel installe le requirements.txt situé À CÔTÉ de la
# fonction (api/requirements.txt), qui référence ce fichier.
fastapi>=0.115
uvicorn[standard]>=0.30
psycopg[binary]>=3.2
python-dotenv>=1.0
